import { CallAutomationEditForm } from '@components/callAutomation';
import { CenteredContainer } from '@components/common';

export const CallAutomationContainer = () => {
    return (
        <CenteredContainer>
            <CallAutomationEditForm />
        </CenteredContainer>
    );
};
